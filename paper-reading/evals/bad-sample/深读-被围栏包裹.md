<!-- fixture: 故意违规，勿修 —— 埋错：整篇被 ``` 代码围栏包裹（E-DEEP-WRAP；因首个非空行是围栏行，E-DEEP-H1 会随之级联报出） -->
```markdown
# Zephyr：面向长文档问答的分层记忆压缩

本文提出 Zephyr，一种在不重训练检索器的前提下压缩长文档上下文的分层记忆机制。方法先把文档切分为语义块并构建两级索引，查询时用粗粒度索引定位候选块，再用细粒度打分与关键词重合度筛选，从而在保持召回率的同时降低推理成本与显存占用。在三个公开问答数据集上，Zephyr 相对固定窗口基线把端到端延迟降低约四成，召回率仅下降一点二个百分点，并且在跨领域零样本迁移测试中保持稳定，说明免训练的分层压缩具备实际可用性。[原文]

## 一、研究背景与动机

长文档问答的常见做法是把全文拼进上下文窗口，显存与延迟随文档长度线性增长。[原文]

## 二、符号与记号表

| 符号 | 含义 |
| --- | --- |
| $e_i$ | 第 i 个句子的句向量 |
| $B$ | 上下文预算，以词元计 |

## 三、方法详解

切分阶段按相邻句子的相似度做合并，压缩阶段按边际增益贪心选块直到预算用尽。[原文]

## 四、实验验证

在三个公开数据集上对比固定窗口与全文拼接两种基线。[实验]

## 五、讨论与贡献

把切分、双粒度索引、加权排序与贪心压缩组合成一条不更新参数的完整流程。[原文]

## 六、局限性与未来工作

评测只覆盖英文抽取式问答，中文与多语言场景尚未验证。[原文]

## 七、证据与出处

- [原文] 方法与实验设定取自论文正文。
- [实验] 延迟与召回率数值取自论文实验章节。
```
